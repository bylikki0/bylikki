model DailyMetric {
  date  DateTime
  key   String
  value Int      @default(0)

  @@id([date, key])
  @@index([key, date])
}

model SearchMiss {
  term  String   @id
  count Int      @default(1)
  lastSeenAt DateTime @updatedAt
  createdAt  DateTime @default(now())

  @@index([count])
}
